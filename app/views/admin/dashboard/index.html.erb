<h1>Welcome</h1>

<h3>All Orders: </h3>

<h4>Packaged Orders:</h4>
<% @orders.each do |order| %>
  <section id="packaged_order-<%= order.id %>">
    <% if order.status == 'packaged' %>
      <p><%= link_to order.user.name, "/admin/users/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Created On: <%= order.created_at.strftime("%m-%d-%Y")%></p>
      <p><%= order.status %></p>
      <%= button_to "Ship Order", "/admin/orders/#{order.id}", method: :patch  %>
    <% end %>
  </section>
<% end %>
<br>
<h4>Pending Orders:</h4>
<% @orders.each do |order| %>
  <section id="pending_order-<%= order.id %>">
    <% if order.status == 'pending' %>
      <p><%= link_to order.user.name, "/admin/users/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Created On: <%= order.created_at.strftime("%m-%d-%Y")%></p>
      <p><%= order.status %></p>
    <% end %>
  </section>
<% end %>
<br>

<h4>Shipped Orders:</h4>
<% @orders.each do |order| %>
  <section id="shipped_order-<%= order.id %>">
    <% if order.status == 'shipped' %>
      <p><%= link_to order.user.name, "/admin/users/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Created On: <%= order.created_at.strftime("%m-%d-%Y")%></p>
      <p><%= order.status %></p>
    <% end %>
  </section>
<% end %>
<br>
<h4>Cancelled Orders:</h4>
<% @orders.each do |order| %>
  <section id="cancelled_order-<%= order.id %>">
    <% if order.status == 'cancelled'%>
      <p><%= link_to order.user.name, "/admin/users/#{order.user.id}" %></p>
      <p>Order ID: <%= order.id %></p>
      <p>Created On: <%= order.created_at.strftime("%m-%d-%Y")%></p>
      <p><%= order.status %></p>
    <% end %>
  </section>
<% end %>
